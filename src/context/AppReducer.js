// Reducer is used to specify any state changes in response to any actions and changes to the store

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
